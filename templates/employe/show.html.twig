{% extends 'base.html.twig' %}

{% block title %}employé{% endblock %}

{% block body %}
    <h1>Employé</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ employe.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ employe.prenom }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ employe.cin }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary"  href="{{ path('app_employe_index') }}">retour à la liste</a>

    <a class="btn btn-outline-success" href="{{ path('app_employe_edit', {'id': employe.id}) }}">modifier</a>

    {{ include('employe/_delete_form.html.twig') }}
{% endblock %}
