{% extends 'base.html.twig' %}

{% block title %}Modifier produit{% endblock %}

{% block body %}
    <h1><title>Produit mod</title></h1>
    <h1 style="font-family:'Microsoft Tai Le'">Modifier le produit d'id = {{ produit.id }}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

{#    <a href="{{ path('app_produit_delete', {'id': produit.id}) }}" class="btn btn-dark" style="margin-top: 10px">Supprimer</a>#}
    <a href="{{ path('app_produit_index') }}" class="btn btn-secondary" style=" margin-top: 10px">Retour</a>

    {#    {{ include('produit/_delete_form.html.twig') }}#}
{% endblock %}
