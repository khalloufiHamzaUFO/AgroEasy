{% extends 'base.html.twig' %}

{% block title %}Comptabilite{% endblock %}

{% block body %}
    <h1>Comptabilite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comptabilite.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ comptabilite.dateComptabilite ? comptabilite.dateComptabilite|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ comptabilite.valeur }}</td>
            </tr>
        </tbody>
    </table>

   
   
    <p><a class="btn btn-outline-success" href="{{ path('app_comptabilite_index', {'id': comptabilite.id}) }}">Retour</a></p>

    <p> <a class="btn btn-outline-info" href="{{ path('app_comptabilite_edit', {'id': comptabilite.id}) }}">modifier</a></p>

    {{ include('comptabilite/_delete_form.html.twig') }}
{% endblock %}
